Whenever you write any Lua code in this conversation,
assume it is **Luau** intended to run in **Roblox Studio** and is part of a
**Rojo-managed project**.

Constraints:
- Use Luau syntax only
- Assume Roblox services are available via `game:GetService`
- The file may be required as a ModuleScript
- Do NOT use standard Lua libraries that are unavailable in Roblox
  (e.g. `io`, `os`, filesystem access)
